name: CronJob Workflow

on:
  push:
    branches:
      - main

jobs:
  first-workflow:
    runs-on: windows-latest

    steps:
      - name: Cronjob Steps
        run: |
          echo "Hello Cron Job"
          echo "Hello cron job again!"
