name: Manual Workflow

on:
  workflow_dispatch:
jobs:
  manual-workflow:
    runs-on: windows-latest

    steps:
      - name: First Step
        run: |
          echo "Hello World by Manual!"
          echo "Hello World again!"
